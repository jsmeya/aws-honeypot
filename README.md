# aws-honeypot
EC2 instance in AWS paired with the T-Pot framework.
The writeup will be displayed in this README as the project develops.

---

Right now, I'm spinning up the EC2 instance through AWS and configuring security groups. Just want to make sure I can connect and set up everything BEFORE it becomes a real target.
